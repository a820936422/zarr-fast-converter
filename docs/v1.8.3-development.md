# Fast NC Zarr v1.8.3 开发文档

- **目标版本**：v1.8.3
- **基线版本**：v1.8.2（已完成并收敛，v1.8.2 开发文档已退役）
- **文档更新**：2026-08-20
- **关联文档**：[维护文档](MAINTENANCE.md)、[模块与运行指南](README.md)
- **测试数据根目录**：`/run/media/owen/HDD/数据备份/`（只读使用）

本文档记录 v1.8.3 的后续待处理内容。v1.8.3 重点是**时间维度信息识别能力加强**（用户反馈：程序尚无法从 MCD12C1 等数据中正确识别时间维度信息）。v1.8.2 已完成内容已收敛进 [维护文档](MAINTENANCE.md)；硬件前置项（真实设备 staging/scaling 外业）继续承接至 3.4。

---

## 1. 现状差距（已实证，2026-08-20 勘察）

| # | 差距 | 证据 |
|---|---|---|
| 1 | HDF-EOS 数据内部时间元数据不被解析：MCD12C1 无 time 维度，但 `CoreMetadata.0` 含 RANGEDATETIME（`RANGEBEGINNINGDATE=2001-01-01`、`RANGEENDINGDATE=2001-12-31` 年度覆盖、`PRODUCTIONDATETIME=2022-05-26T17:04:09Z`）；现有 CoreMetadata 解析仅用于 Swath 空间结构 | 备份语料文件属性提取 |
| 2 | filename 自动模板推断在 MCD12C1 目录失败：13 位生产时间戳（`2022146170409`）与观测字段（`A2001001`）并存触发 `_automatic_rule_candidates` 整体歧义拒绝，需手动 `field_values` | `inspect_source(mode=filename)` 实测 FilenameTimeError |
| 3 | 手动模板成功时时间轴仅单日 `2001-01-01`，丢失「年度覆盖 2001-01-01~2001-12-31」语义 | inventory/report 实测 |
| 4 | CF calendar 支持面窄：`_SUPPORTED_CALENDARS={standard,gregorian,proleptic_gregorian,julian}`，noleap/360_day 等在 inspection 即失败 | `inspection.py:127` |

## 2. v1.8.3 目标

1. **从数据内部识别权威时间**：解析 HDF-EOS `CoreMetadata.0` RANGEDATETIME（含生产时间戳），进入 inventory 证据与一致性校验。
2. **文件名自动推断抗歧义**：生产/处理时间戳字段（13/14 位 `YYYYDDDHHMMSS`/`YYYYMMDDHHMMSS`）识别并排除出歧义集，MCD12C1 目录自动模板推断成功。
3. **时间语义正确表达**：覆盖区间（beg~end）与时间尺度进入 inventory/报告，不再把年度产品标成单日。
4. **CF 日历识别面评估**：noleap/360_day 等识别决策明确（支持或明确回退原因）。

## 3. 待办清单

### 3.1 HDF-EOS 数据内部时间识别（重点）

- [ ] **CoreMetadata RANGEDATETIME 解析**
  - 新增 PDS 对象语法解析：`RANGEBEGINNINGDATE/TIME`、`RANGEENDINGDATE/TIME`、`PRODUCTIONDATETIME`、`LOCALGRANULEID`（放 `filename_mode.py` 与 Swath 解析并列，或抽公共 helper）；
  - 对 HDF 引擎源在 inspect 阶段读取参考文件，得到权威覆盖区间（date/覆盖开始结束）。
- [ ] **Inventory 证据与一致性**
  - Inventory 新增 `coverage_start/coverage_end`、`internal_time_source` 字段（含 `source_*` 风格元数据），进入 inventory 报告与 pipeline manifest；
  - 文件名推断与内部时间不一致 → inspection warning（不静默替换，内部时间为权威记录）。

### 3.2 文件名自动推断抗歧义

- [ ] **生产时间戳字段识别**
  - `_automatic_rule_candidates` / `_repeated_date_like_positions` 扩展：13 位 `YYYYDDDHHMMSS`、14 位 `YYYYMMDDHHMMSS` 等 process-metadata 形态从歧义集排除（与 `LOCALGRANULEID` 生产时间一致）；
  - 验收：MCD12C1 目录 `inspect_source(mode="filename")` 自动推断成功且取观测字段。
- [ ] **内部时间消歧保底**
  - 歧义仍存在但候选含唯一可解析日期 + 已取得内部时间时，以内部时间消歧并记录 reason（不伪装成功）。

### 3.3 CF 时间元数据与日历识别面

- [ ] **日历识别面评估**
  - noleap/360_day/all_leap：扩展 `_SUPPORTED_CALENDARS` 识别（解码沿用 netCDF4/xarray 兼容路径）或明确「识别并记录 calendar，转换按既有回退策略」；产出决策记录；
- [ ] **时间坐标识别补强**
  - 评估标量时间属性型/非标准 time 变量名等场景，范围控制，不做不必要的新路径。

### 3.4 硬件前置（承接 v1.8.1/v1.8.2，见维护文档 6.2 第 7 条）

- [ ] **真实设备读写阶段分离验证**（`benchmark_scaling.py --staging-root` 跨设备 A/B；需更大数据窗口与独立 SSD/NVMe）
- [ ] **真实数据 scaling 基准外业验证**（更大窗口 + 多设备类型，结果纳入发布门禁）

### 3.5 发布与收尾

- [ ] **v1.8.3 发布门禁**
  - `version-check`、`contract-check`、`python-lint`、`cross-backend-test`、`desktop-typecheck`、`desktop-build`、`rust-fmt-check`、`rust-clippy`、`rust-test`、`rust-desktop-test`、`test`（全量回归）、`native-check`；sidecar 重建校验（Python 有改动）、`tauri-build`、`release-candidate`。
- [ ] **v1.8.3 收尾清理**
  - 完成时清理本文档，将完成项收敛进 `MAINTENANCE.md` 版本历史与已验证状态。

## 4. 验收标准（v1.8.3 完成定义）

1. MCD12C1 目录无需手动模板即可识别时间，推断日期与 `CoreMetadata RANGEBEGINNINGDATE` 一致；年度覆盖区间记录正确（acceptance 通过：`mcd12c1_auto_time_recognition`）。
2. 生产时间戳抗歧义 synthetic 单测通过；既有回归（295 passed 基线）+ 新增用例全绿。
3. 日历识别面有明确决策记录（支持或回退原因）。
4. 版本/契约/文档一致；发布门禁全部通过；硬件前置项保持待办。