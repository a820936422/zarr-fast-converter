# Fast NC Zarr v1.8.2 开发文档

- **目标版本**：v1.8.2
- **基线版本**：v1.8.1（已完成并收敛，v1.8.1 开发文档已退役）
- **文档更新**：2026-08-20
- **关联文档**：[维护文档](MAINTENANCE.md)、[模块与运行指南](README.md)
- **测试数据根目录**：`/run/media/owen/HDD/数据备份/`（只读使用）

本文档记录 v1.8.2 的后续待处理内容。v1.8.1 已完成内容已收敛进 [维护文档](MAINTENANCE.md) 版本历史与已验证状态；v1.8.1 未完成的硬件前置项统一承接至本文档 3.4。

---

## 1. v1.8.1 已完成内容（基线）

- filename 缺日检测与重复文件边界（拒绝语义，绝不静默去重）。
- filename 模式 PipelineFusion 单遍流式（crop ≤ 2 GiB、串行/auto 重采样；`--no-fusion` 与磁盘 checkpoint/恢复语义不变）。
- 真实 HDF4-EOS（GLASS 打包 int16）、GeoTIFF（GOSIF）、HDF-EOS 多分类（MCD12C1）、NetCDF4（FLUXSAT）小窗验收 6/6，含 `gosif_filename_duplicate_rejection`。
- v1.8.1 发布门禁（sidecar 重建校验、typecheck/build/rust/tauri-build/release-candidate；全量回归 295 passed）。

---

## 2. v1.8.2 目标

1. 用备份语料真实数据收敛 filename 模式 PipelineFusion 与格式验证（不依赖硬件）。
2. 补齐格式验证盲区：GeoTIFF CRS 旋转 / 多 band、CF packing（scale/add_offset）打包整数等 synthetic fixture 验收。
3. 可选：Python 大模块可维护性拆分（行为保持，全量回归门禁）。
4. 承接 v1.8.1 硬件前置项并纳入发布门禁。

---

## 3. 待办清单

### 3.1 真实数据收敛（本轮重点，无需硬件）

- [x] **filename 模式 PipelineFusion 真实数据验收**
  - `tests/external_data/run_v182_acceptance.py`（沿用 run_v181 只读链接 + 受控小窗模式）：
    - `gosif_filename_fusion_parity`：GOSIF GeoTIFF 3 时间步 convert→resample，融合 vs 磁盘路径逐值 parity（EXACT，含坐标与 time 轴），`fused_in_memory`/`write_amplification=1.0`/无 `source-crop.zarr`；
    - `glass_filename_fusion_gap_parity`：HDF4 打包 int16 含缺日（2001017）批次，融合与磁盘路径 fill 一致（4 时间步 EXACT parity）。
  - 顺带修复：`build_filename_fused_dataset` 现对内存 crop 执行 `xr.decode_cf`，等价于磁盘路径重开时的 `mask_and_scale=True`（打包源 fill→NaN、物理值），修复了 CF 打包源上融合路径直接插值原始打包值的正确性缺口。
- [x] **可选：更多真实语料融合覆盖**
  - GLASS HDF4 打包 int16（scale_factor）小窗融合 parity 已由 `glass_filename_fusion_gap_parity` 覆盖并通过。

### 3.2 格式验证盲区补齐（fixture）

- [x] **GeoTIFF CRS 旋转与多 band fixture**
  - `synthetic_rotated_geotiff_regular_grid`：带旋转仿射变换的 GeoTIFF 被接受，坐标按规则网格重建（不做地理重投影），转换值逐像素与栅格原始窗口读取一致；
  - `synthetic_multiband_geotiff_rejected`：多 band GeoTIFF 在 filename 模式被确定性拒绝（`变量 … 含有多个 band；当前文件名时间模式暂只支持单 band`），拒绝原因记录进 acceptance。
- [x] **CF packing（scale_factor/add_offset）打包整数**
  - `synthetic_packed_preserved_convert`：int16 + scale_factor/add_offset 打包整数在转换中保留打包值与 CF attrs（rest 态不解码）；native 打包 int16 拒绝、重采样路径按 `mask_and_scale` 解码为物理值（`glass_filename_fusion_gap_parity` 覆盖），capability 矩阵一致。

### 3.3 可维护性（可选，行为保持）

- [x] **Python 大模块拆分（决策：本周期推迟）**
  - 结论：`filename_mode.py`（≈2700 行）、`application/services.py`、`pipeline/engine.py` 拆分属于行为保持的大范围重构，与 v1.8.2 收敛性目标冲突、风险收益比不佳；按预案标记推迟到后续版本，保持 backlog 记录。

### 3.4 硬件前置（承接 v1.8.1，见维护文档 6.2 第 7 条）

- [ ] **真实设备读写阶段分离验证**
  - `scripts/benchmark_scaling.py --staging-root` 跨设备 A/B；正式收益需更大数据窗口与独立 SSD/NVMe 设备。
- [ ] **真实数据 scaling 基准外业验证**
  - FLUXSAT/GLASS 更大窗口 + HDD/SSD/NVMe/内存受限容器；结果纳入发布门禁。

### 3.5 发布与收尾

- [x] **v1.8.2 发布门禁**
  - `version-check`（1.8.2 全字段一致）、`contract-check`、`python-lint`、`cross-backend-test`（44 passed）、`desktop-typecheck`、`desktop-build`、`rust-fmt-check`、`rust-clippy`、`rust-test`、`rust-desktop-test`、`test`（全量 295 passed）、`native-check`（`ready_for_p0=true`）全部通过；
  - sidecar 重建校验（含 filename fusion CF-decode 修复，capabilities smoke：accepted → started → finished）；`tauri-build` 产出本地 `Fast NC Zarr_1.8.2_amd64.deb`；`release-candidate` 已收集并更新 SHA256SUMS。
- [x] **v1.8.2 收尾清理**
  - 本文档完成项已标记；开发内容收敛进 `MAINTENANCE.md` 已验证状态与发布状态；硬件前置项保持待办。

---

## 4. 验收标准（v1.8.2 完成定义）

1. filename fusion 真实数据 parity（GOSIF + 缺日 fill）通过并记录进 acceptance 报告。✅（`run_v182_acceptance.py` `gosif_filename_fusion_parity` / `glass_filename_fusion_gap_parity`，EXACT parity）
2. 格式盲区（GeoTIFF CRS 旋转/多 band、CF packing）有 fixture 验收，或明确记录不支持/回退原因。✅（旋转接受+规则网格重建记录、多 band 确定性拒绝+reason、打包保留+解码覆盖）
3. 可选拆分（若做）完成且全量回归保持绿色。✅（本周期决策推迟，回归 295 passed）
4. 版本/契约/文档一致；发布门禁全部通过；硬件前置项保持明确待办。✅（version/contract/lint/cross-backend/desktop/rust/native/sidecar/tauri-build/release-candidate 全通过；3.4 两项保持「硬件前置」待办）
