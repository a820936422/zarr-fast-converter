# Fast NC Zarr v1.8.2 开发文档

- **目标版本**：v1.8.2
- **基线版本**：v1.8.1（已完成并收敛，v1.8.1 开发文档已退役）
- **文档更新**：2026-08-20
- **关联文档**：[维护文档](MAINTENANCE.md)、[模块与运行指南](README.md)
- **测试数据根目录**：`/run/media/owen/HDD/数据备份/`（只读使用）

本文档记录 v1.8.2 的后续待处理内容。v1.8.1 已完成内容已收敛进 [维护文档](MAINTENANCE.md) 版本历史与已验证状态；v1.8.1 未完成的硬件前置项统一承接至本文档 3.4。

---

## 1. v1.8.1 已完成内容（基线）

- filename 缺日检测与重复文件边界（拒绝语义，绝不静默去重）。
- filename 模式 PipelineFusion 单遍流式（crop ≤ 2 GiB、串行/auto 重采样；`--no-fusion` 与磁盘 checkpoint/恢复语义不变）。
- 真实 HDF4-EOS（GLASS 打包 int16）、GeoTIFF（GOSIF）、HDF-EOS 多分类（MCD12C1）、NetCDF4（FLUXSAT）小窗验收 6/6，含 `gosif_filename_duplicate_rejection`。
- v1.8.1 发布门禁（sidecar 重建校验、typecheck/build/rust/tauri-build/release-candidate；全量回归 295 passed）。

---

## 2. v1.8.2 目标

1. 用备份语料真实数据收敛 filename 模式 PipelineFusion 与格式验证（不依赖硬件）。
2. 补齐格式验证盲区：GeoTIFF CRS 旋转 / 多 band、CF packing（scale/add_offset）打包整数等 synthetic fixture 验收。
3. 可选：Python 大模块可维护性拆分（行为保持，全量回归门禁）。
4. 承接 v1.8.1 硬件前置项并纳入发布门禁。

---

## 3. 待办清单

### 3.1 真实数据收敛（本轮重点，无需硬件）

- [ ] **filename 模式 PipelineFusion 真实数据验收**
  - 新增 `tests/external_data/run_v182_acceptance.py`（沿用 run_v181 只读链接 + 受控小窗模式）：
    - GOSIF GeoTIFF 小窗 convert→resample：融合路径（fusion on）与磁盘路径（fusion off）逐值 parity（EXACT，含坐标与 time 轴）；
    - 缺日批次（`missing_time_keys` 非空）融合输出与磁盘路径 fill 一致；
  - 记录 manifest 证据：`stages.conversion.status=fused_in_memory`、`logical_io.write_amplification=1.0`、无中间 `source-crop.zarr`。
- [ ] **可选：更多真实语料融合覆盖**
  - GLASS HDF4 打包 int16（scale_factor）小窗融合 parity，验证 filename 融合对 packed 数据不破坏 scale/fill 语义。

### 3.2 格式验证盲区补齐（fixture）

- [ ] **GeoTIFF CRS 旋转与多 band fixture**
  - 备份语料未覆盖「带旋转的仿射变换」与「多 band」GeoTIFF；构造合成 fixture 补 conversion/重采样验收，或明确记录不支持/回退原因（沿用验收标准“有 fixture 验收或明确记录原因”）。
- [ ] **CF packing（scale_factor/add_offset）打包整数**
  - 真实/合成打包整数 source：native 回退兼容路径与逐值验收记录，保持 capability 矩阵一致。

### 3.3 可维护性（可选，行为保持）

- [ ] **Python 大模块拆分**
  - `src/fast_nc_zarr/filename_mode.py`（≈2600 行）、`application/services.py`（≈1280 行）、`pipeline/engine.py`（≈1900 行）按职责拆分；
  - 约束：纯移动/重组、不改行为；拆分后 `pixi run test` 保持绿色（当前 295 passed）；
  - 风险预案：若重构风险过高，可仅拆分 `filename_mode.py` 或标记推迟到后续版本。

### 3.4 硬件前置（承接 v1.8.1，见维护文档 6.2 第 7 条）

- [ ] **真实设备读写阶段分离验证**
  - `scripts/benchmark_scaling.py --staging-root` 跨设备 A/B；正式收益需更大数据窗口与独立 SSD/NVMe 设备。
- [ ] **真实数据 scaling 基准外业验证**
  - FLUXSAT/GLASS 更大窗口 + HDD/SSD/NVMe/内存受限容器；结果纳入发布门禁。

### 3.5 发布与收尾

- [ ] **v1.8.2 发布门禁**
  - `version-check`、`contract-check`、`python-lint`、`cross-backend-test`、`desktop-typecheck`、`desktop-build`、`rust-fmt-check`、`rust-clippy`、`rust-test`、`rust-desktop-test`、`test`（全量回归）、`native-check`；sidecar 重建校验（`desktop-sidecar`、`sidecar-check`）、`tauri-build`、`release-candidate`。
- [ ] **v1.8.2 收尾清理**
  - 完成时清理本文档，将完成项收敛进 `MAINTENANCE.md` 版本历史与已验证状态。

---

## 4. 验收标准（v1.8.2 完成定义）

1. filename fusion 真实数据 parity（GOSIF + 缺日 fill）通过并记录进 acceptance 报告。
2. 格式盲区（GeoTIFF CRS 旋转/多 band、CF packing）有 fixture 验收，或明确记录不支持/回退原因。
3. 可选拆分（若做）完成且全量回归保持绿色。
4. 版本/契约/文档一致；发布门禁全部通过；硬件前置项保持明确待办。
