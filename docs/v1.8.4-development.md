# Fast NC Zarr v1.8.4 开发文档

- **目标版本**：v1.8.4
- **基线版本**：v1.8.3（已完成并收敛，v1.8.3 开发文档已退役）
- **文档更新**：2026-08-20
- **关联文档**：[维护文档](MAINTENANCE.md)、[模块与运行指南](README.md)、[GUI 接口对齐审计报告](gui-audit-v1.8.4.md)
- **测试数据根目录**：`/run/media/owen/HDD/数据备份/`（只读使用）

本文档记录 v1.8.4 的后续待处理内容。v1.8.4 重点是**桌面 GUI 接口对齐审计与界面重构**：全面核对前端接口与后端契约，重新设计界面结构与交互逻辑，提升个人用户使用便利性。v1.8.3 已完成内容已收敛进 [维护文档](MAINTENANCE.md)（时间维度识别、变量完整性、融合等）；硬件前置项（真实设备 staging/scaling 外业）继续承接至 3.5。

---

## 1. v1.8.3 已完成内容（基线）

- 时间维度识别：HDF-EOS `CoreMetadata.0` RANGEDATETIME 解析（coverage 字段 + 一致性 warning）、生产时间戳 13/14 位抗歧义、年度产品时间尺度（每年 1 点）、日历识别面（noleap 族支持、360_day 明确拒绝）。
- 变量完整性：额外维度数值变量不再静默丢弃（报告/warning/选择显式拒绝，GUI 变量面板可见）。
- `run_v183_acceptance.py` 1/1；全量回归 302 passed；发布门禁通过（local `Fast NC Zarr_1.8.3_amd64.deb`）。

## 2. v1.8.4 目标

1. **GUI 接口对齐审计**：字段/命令/事件/capability 四层审计，产出 `docs/gui-audit-v1.8.4.md`，缺口全部修复并加协议测试。
2. **UI 结构重构**：拆分 1828 行单体 `App.tsx` 为 views/components/lib，行为保持、typecheck 每步绿。
3. **交互逻辑重设计**：向导式步骤、按输入类型的操作选择（含纯转换入口）、变量面板增强、计划/任务中心可视化增强、统一错误处理。
4. **版本一致与发布门禁**：保持全量回归绿色（302 passed 基线），发布门禁通过。

## 3. 待办清单

### 3.1 GUI 接口对齐审计

- [ ] **四层审计**（产出 `docs/gui-audit-v1.8.4.md`）
  - 字段层：`api.ts` payload 字段并集 ↔ `desktop_worker/worker.py` 解析字段并集差集表；
  - 命令层：Tauri 16 command ↔ `api.ts` 调用 ↔ `contracts/README.md` 命令清单；
  - 事件层：`taskEvents.ts` 监听载荷 ↔ Rust `tasks.rs` 扇出 ↔ contract event fixtures；
  - capability 层：native 报告 ↔ GUI `supported()` 禁用逻辑 ↔ capability fixture。
- [ ] **审计脚本**：`scripts/check_gui_api_alignment.py`（只读，可持续复用），输出差集报告。
- [ ] **已知缺口修复**
  - `fusion` 未暴露：`_pipeline_config` 解析 `payload.fusion`（默认 true）+ GUI 处理配置暴露「内存单遍流式融合」开关与说明；
  - 纯转换入口：操作选择器支持「仅转换」（raw 输入无需 resample/rechunk/recompress）；
  - 恢复入口：确认/补齐任务中心「从临时目录继续」（`inspectPipelineRecovery` + `resumePipeline` 的 GUI 交互）；
  - snapshot 类型化：按 schema_version 3 定义 `InspectionSnapshot` TS 类型，替换关键 `asRecord` 访问；
  - 其余差集项逐一处置并加 `tests/test_protocol.py` 契约用例。

### 3.2 UI 结构重构（行为保持）

- [ ] `src/lib/`：`payloads.ts`（pipeline/inspection 请求构建）、`validate.ts`、`format.ts`、`constants.ts`、`types.ts`；
- [ ] `src/components/`：`Sidebar`、`Topbar`、`FlowStages`、`VariablePanel`、`TimeRuleModal`、`PlanReport`、`NativePanel`、`TaskList`、`ReportViewer`、`PathPicker`、`ErrorBanner`；
- [ ] `src/views/`：`OverviewView`、`InspectionView`、`PipelineView`、`TasksView`、`SettingsView`；
- [ ] 每步合并后 `desktop-typecheck`/`desktop-build` 绿；styles.css 演进保持视觉基线。

### 3.3 交互逻辑重设计

- [ ] 向导式步骤条：输入 → 时间确认 → 处理配置 → 计划预览 → 执行（按输入类型动态）；
- [ ] 操作选择器：转换/重采样/重分块/重压缩四段开关 + 仅转换场景 + fusion 开关；
- [ ] 变量面板增强：全选/反选/搜索/批量重命名/排除变量展示；
- [ ] 计划预览：operation_decisions、backend/fallback reason、资源估算、警告分色；
- [ ] 任务中心：阶段 timeline、进度、manifest 摘要、失败展开、恢复入口；
- [ ] 统一 ErrorBanner（展开 + 重试）。

### 3.4 GUI 手工验收清单

- [ ] MCD12C1 全流程：自动时间识别、9 变量清单（含 3 个排除项）、年度时间轴、仅转换入口；
- [ ] GOSIF filename 模式转换+重采样；
- [ ] GLASS 打包 int16 转换+重采样；
- [ ] Zarr 输入重分块/重压缩/重采样；
- [ ] 任务取消、失败展示、恢复入口；
- [ ] 融合开关开/关两态下计划与 manifest 证据。

### 3.5 硬件前置（承接 v1.8.1~v1.8.3，见维护文档 6.2 第 7 条）

- [ ] **真实设备读写阶段分离验证**（`benchmark_scaling.py --staging-root` 跨设备 A/B；需更大数据窗口与独立 SSD/NVMe）
- [ ] **真实数据 scaling 基准外业验证**（更大窗口 + 多设备类型，结果纳入发布门禁）

### 3.6 发布与收尾

- [ ] **v1.8.4 发布门禁**
  - `version-check`、`contract-check`、`python-lint`、`cross-backend-test`、`desktop-typecheck`、`desktop-build`、`rust-fmt-check`、`rust-clippy`、`rust-test`、`rust-desktop-test`、`test`（全量回归）、`native-check`；`tauri-build`、`release-candidate`（worker 有改动则先重建 sidecar）。
- [ ] **v1.8.4 收尾清理**
  - 审计报告、完成项收敛进 `MAINTENANCE.md` 版本历史与已验证状态；GUI 重构验收清单勾选。

## 4. 验收标准（v1.8.4 完成定义）

1. `docs/gui-audit-v1.8.4.md` 四层审计报告落地；所有缺口修复；新增协议测试通过。
2. `App.tsx` 拆分完成（views/components/lib），typecheck/build 绿，行为等价（Python 回归 302 + 前端构建）。
3. 向导式流程、操作选择器（含仅转换/融合开关/恢复入口）、变量面板、任务中心增强全部可用（手工验收清单通过）。
4. 版本/契约/文档一致；发布门禁全部通过；本地候选包收集；硬件前置项保持待办。