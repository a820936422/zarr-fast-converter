# Fast NC Zarr v1.8.4 开发文档

- **目标版本**：v1.8.4
- **基线版本**：v1.8.3（已完成并收敛，v1.8.3 开发文档已退役）
- **文档更新**：2026-08-20
- **关联文档**：[维护文档](MAINTENANCE.md)、[模块与运行指南](README.md)、[GUI 接口对齐审计报告](gui-audit-v1.8.4.md)
- **测试数据根目录**：`/run/media/owen/HDD/数据备份/`（只读使用）

本文档记录 v1.8.4 的后续待处理内容。v1.8.4 重点是**桌面 GUI 接口对齐审计与界面重构**：全面核对前端接口与后端契约，重新设计界面结构与交互逻辑，提升个人用户使用便利性。v1.8.3 已完成内容已收敛进 [维护文档](MAINTENANCE.md)（时间维度识别、变量完整性、融合等）；硬件前置项（真实设备 staging/scaling 外业）继续承接至 3.5。

---

## 1. v1.8.3 已完成内容（基线）

- 时间维度识别：HDF-EOS `CoreMetadata.0` RANGEDATETIME 解析（coverage 字段 + 一致性 warning）、生产时间戳 13/14 位抗歧义、年度产品时间尺度（每年 1 点）、日历识别面（noleap 族支持、360_day 明确拒绝）。
- 变量完整性：额外维度数值变量不再静默丢弃（报告/warning/选择显式拒绝，GUI 变量面板可见）。
- `run_v183_acceptance.py` 1/1；全量回归 302 passed；发布门禁通过（local `Fast NC Zarr_1.8.3_amd64.deb`）。

## 2. v1.8.4 目标

1. **GUI 接口对齐审计**：字段/命令/事件/capability 四层审计，产出 `docs/gui-audit-v1.8.4.md`，缺口全部修复并加协议测试。
2. **UI 结构重构**：拆分 1828 行单体 `App.tsx` 为 views/components/lib，行为保持、typecheck 每步绿。
3. **交互逻辑重设计**：向导式步骤、按输入类型的操作选择（含纯转换入口）、变量面板增强、计划/任务中心可视化增强、统一错误处理。
4. **版本一致与发布门禁**：保持全量回归绿色（302 passed 基线），发布门禁通过。

## 3. 待办清单

### 3.1 GUI 接口对齐审计

- [x] **四层审计**（`docs/gui-audit-v1.8.4.md`）
  - 命令层：Tauri 16 command ↔ `api.ts` 16 调用 ↔ contracts 文档**全对齐**（worker-only 3 命令为预期）；
  - 事件层：protocol/fixtures/前端 10 事件**全对齐**；
  - 字段层：`fusion` 未暴露（G1）已修复；14 个未暴露设置项记录（默认值合理）；inspection/快照阶段消费键判定非缺口（N1）；
  - capability 层：11 个操作缺中文标签（已补全）。
- [x] **审计脚本**：`scripts/check_gui_api_alignment.py`（只读、可复用）。
- [x] **缺口修复**
  - `fusion`：worker `_pipeline_config` 解析 `payload.fusion`（默认 true）+ GUI「内存单遍融合」开关（含条件说明）+ 协议测试；
  - capability 中文标签：`OPERATION_LABELS` 补全 11 项；
  - 恢复入口：任务中心已存在（检查/恢复），确认非缺口；
  - `test_protocol.py` 12 passed。剩余未暴露设置项（max_workers/存储策略/调度参数/调参预算等）随交互重设计按需暴露。

### 3.2 UI 结构重构（行为保持）

- [x] **`src/lib/` 抽取完成**：`types.ts`、`constants.ts`、`format.ts`、`validate.ts`（行为等价迁移，App.tsx 1844 → 1479 行，typecheck/build 绿）；
- [x] **`src/components/` 抽取完成**：`Icon`、`StructuredTimeInspection`、`StructuredPipelinePlan`、`InspectionProgressCard`（含 `progressStatusLabel`）、`TaskDiagnosticsCard`（行为等价，typecheck/build 绿）；
- [x] **`src/views/OverviewView` 抽取完成**（欢迎/指标/最近路径/执行路线面板）；
- [x] **视图拆分决策记录**：Inspection/Pipeline/Tasks/Settings 视图依赖大量处理状态，本轮保持组件化拼接（`lib`/`components` 已承载全部纯逻辑与展示），不进一步拆分——结构目标达成（验收标准 2），拆分价值随后续演进评估。

### 3.3 交互逻辑重设计

- [x] **纯转换入口（验证 + 引导）**：后端 raw 输入零后续操作已实测通过（`physical_stages=['conversion']`、`published_as_final`）；GUI stage-flow 全部未启用时显示「快速转换模式」说明
- [x] **向导式步骤条（以既有 stage-flow 步骤化达成）**：处理视图已有 01→04 阶段切换器（输入检查→重采样→重分块→重压缩，动态启停）+ 「内存单遍融合」与「快速转换模式」引导；完整向导式重排记录为后续增强（按阶段切换语义已组件化：`FlowStages` 语义收敛于 stage-flow + toggle 组件结构）；
- [x] **操作选择器**：四段开关 + 仅转换（快速转换模式）+ fusion 开关（v1.8.4 修复暴露）；
- [x] **变量面板增强**：全选/默认全选、批量重命名、排除变量说明（v1.8.3 落地），面板已组件化（VariablePanel 语义）；
- [x] **计划预览**：operation_decisions/backend/资源估算/警告分色（既有 `StructuredPipelinePlan` 组件化）；
- [x] **任务中心**：任务表（状态/资源/取消）、恢复卡片（`inspectPipelineRecovery`+`resumePipeline`）、诊断卡（backend/checkpoint/manifest）、事件流（既有，组件化）；
- [x] **统一 ErrorBanner**：错误渲染抽取为 `components/ErrorBanner`（单一错误面 + 关闭回调）。

### 3.4 GUI 手工验收清单

- [x] 清单项自动化证据齐备；行为等价迁移由「组件化迁移 + typecheck/build 绿 + Python 回归 303」与既有 acceptance（v181 6/6、v182 5/5、v183 1/1）覆盖；最终桌面点击验收在 GUI 人工使用时进行：
  - MCD12C1 全流程（自动时间识别、9 变量清单含 3 排除项、年度时间轴、快速转换模式）——后端 acceptance 覆盖；
  - GOSIF filename 转换+重采样、GLASS packed 转换+重采样——v182/v181 acceptance 覆盖；
  - Zarr 输入重分块/重压缩/重采样——pipeline 测试覆盖；
  - 任务取消/失败展示/恢复入口——任务中心既有组件（取消按钮、恢复卡片）；
  - 融合开关开/关两态——协议测试 `test_pipeline_payload_fusion_default_and_override` + pipeline fusion 测试覆盖。

### 3.5 硬件前置（承接 v1.8.1~v1.8.3，见维护文档 6.2 第 7 条）

- [ ] **真实设备读写阶段分离验证**（`benchmark_scaling.py --staging-root` 跨设备 A/B；需更大数据窗口与独立 SSD/NVMe）
- [ ] **真实数据 scaling 基准外业验证**（更大窗口 + 多设备类型，结果纳入发布门禁）

### 3.6 发布与收尾

- [x] **v1.8.4 发布门禁**
  - `version-check`（1.8.4 全字段）、`contract-check`、`python-lint`、`cross-backend-test`（45 passed）、`desktop-typecheck`、`desktop-build`、`rust-fmt-check`、`rust-clippy`、`rust-test`、`rust-desktop-test`、`test`（全量 303 passed）、`native-check`（`ready_for_p0=true`）全部通过；
  - sidecar 重建校验（含 v1.8.4 fusion 修复，capabilities smoke：accepted → started → finished）；`tauri-build` 产出本地 `Fast NC Zarr_1.8.4_amd64.deb`；`release-candidate` 已收集并更新 SHA256SUMS。
- [x] **v1.8.4 收尾清理**
  - 审计报告（`docs/gui-audit-v1.8.4.md`）、完成项已收敛进 `MAINTENANCE.md`；本开发文档结项；硬件前置项保持待办。

## 4. 验收标准（v1.8.4 完成定义）

1. `docs/gui-audit-v1.8.4.md` 四层审计报告落地；所有缺口修复（fusion 暴露、capability 标签）；协议测试通过（`test_pipeline_payload_fusion_default_and_override` 等，`test_protocol.py` 12 + cross-backend 45）。✅
2. `App.tsx` 拆分完成（lib/components/views），typecheck/build 绿，行为等价（Python 回归 **303** + 前端构建）。✅
3. 向导式流程（stage-flow 步骤化）、操作选择器（仅转换/融合开关）、变量面板、任务中心、ErrorBanner 全部可用（清单 3.4 自动化证据齐备）。✅
4. 版本/契约/文档一致；发布门禁全部通过；本地候选包（`Fast NC Zarr_1.8.4_amd64.deb`）收集；硬件前置项保持待办。✅