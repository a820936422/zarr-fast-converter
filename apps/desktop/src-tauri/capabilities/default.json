{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the desktop shell",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
